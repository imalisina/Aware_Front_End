import 'package:flutter/cupertino.dart';

class JuryDetails extends StatelessWidget {
  const JuryDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}